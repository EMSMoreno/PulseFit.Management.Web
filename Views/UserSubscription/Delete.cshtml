@* @model PulseFit.Management.Web.Models.UserSubscriptionViewModel

@{
    ViewData["Title"] = "Delete Subscription";
}

<div class="container my-5">
    <div class="card shadow-lg p-5 text-center">
        <h2 class="text-danger fw-bold">Delete Subscription</h2>
        <p class="fs-5">Are you sure you want to delete the subscription <strong>@Model.SubscriptionName</strong>?</p>
        <p class="text-muted">This action cannot be undone.</p>

        <form asp-action="DeleteConfirmed" method="post">
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="ClientId" />

            <button type="submit" class="btn btn-danger btn-lg mt-3">Yes, Delete</button>
            <a asp-action="Details" asp-route-clientId="@Model.ClientId" class="btn btn-link mt-3">Cancel</a>
        </form>
    </div>
</div>

<style>
    h2 {
        font-size: 2rem;
    }

    p.fs-5 {
        font-size: 1.2rem;
    }

    .btn-danger {
        transition: background-color 0.3s ease;
    }

        .btn-danger:hover {
            background-color: #dc3545;
        }
</style>
 *@