@* @model PulseFit.Management.Web.Models.UserSubscriptionViewModel

@{
    ViewData["Title"] = "Create Subscription";
}

<<div class="container my-5">
    <div class="card shadow-lg p-4">
        <h2 class="text-primary text-center fw-bold mb-4">Create Subscription for @Model.Client.FirstName</h2>
        <form asp-action="Create" method="post" class="needs-validation" novalidate>
            <input type="hidden" asp-for="ClientId" />

            <!-- Seleção de tipo de subscrição -->
            <div class="mb-3">
                <label asp-for="SubscriptionId" class="form-label">Subscription Type</label>
                <select asp-for="SubscriptionId" asp-items="Model.SubscriptionOptions" class="form-select" required>
                    <option value="">-- Select Subscription --</option>
                </select>
                <span asp-validation-for="SubscriptionId" class="text-danger"></span>
            </div>

            <!-- Data de início -->
            <div class="mb-3">
                <label asp-for="StartDate" class="form-label">Start Date</label>
                <input asp-for="StartDate" class="form-control" type="date" required />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>

            <!-- Pagamento -->
            <div class="form-check form-switch mb-3">
                <input asp-for="IsPaid" class="form-check-input" type="checkbox" />
                <label asp-for="IsPaid" class="form-check-label">Mark as Paid</label>
            </div>

            <div class="mb-3">
                <label asp-for="AmountPaid" class="form-label">Amount Paid (€)</label>
                <input asp-for="AmountPaid" class="form-control" type="number" min="0" step="0.01" required />
                <span asp-validation-for="AmountPaid" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary btn-lg w-100 mt-3">Create Subscription</button>
            <a asp-action="Index" class="btn btn-link d-block text-center mt-3">Back to List</a>
        </form>
    </div>
</div>


<style>
    h2 {
        font-size: 2rem;
    }

    .form-label {
        font-weight: 600;
    }

    .btn-primary {
        transition: background-color 0.3s ease;
    }

        .btn-primary:hover {
            background-color: #0056b3;
        }
</style>
 *@