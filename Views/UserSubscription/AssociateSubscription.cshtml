@* @model PulseFit.Management.Web.Models.UserSubscriptionViewModel

@{
    ViewData["Title"] = "Associate Subscription";
}

<div class="container my-5">
    <div class="card shadow-lg p-4">
        <h2 class="text-primary text-center fw-bold mb-4">Associate Subscription to @Model.Client.FirstName</h2>
        <form asp-action="AssociateSubscription" method="post" class="needs-validation" novalidate>
            <input type="hidden" asp-for="ClientId" />

            <div class="mb-3">
                <label asp-for="SubscriptionId" class="form-label">Select Subscription</label>
                <select asp-for="SubscriptionId" asp-items="Model.SubscriptionOptions" class="form-select" required>
                    <option value="">-- Select Subscription --</option>
                </select>
                <span asp-validation-for="SubscriptionId" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-success btn-lg w-100 mt-3">Associate Subscription</button>
            <a asp-action="Index" class="btn btn-link d-block text-center mt-3">Back to Client List</a>
        </form>
    </div>
</div>

<style>
    h2 {
        font-size: 2rem;
    }

    .card {
        max-width: 600px;
        margin: 0 auto;
    }
</style>
 *@